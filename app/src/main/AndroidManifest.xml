<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Health Connect: ScaleLauncher writes measurements but never reads health data. -->
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT" />
    <uses-permission android:name="android.permission.health.WRITE_HEIGHT" />
    <uses-permission android:name="android.permission.health.WRITE_BODY_FAT" />
    <uses-permission android:name="android.permission.health.WRITE_BODY_WATER_MASS" />
    <uses-permission android:name="android.permission.health.WRITE_BONE_MASS" />
    <uses-permission android:name="android.permission.health.WRITE_LEAN_BODY_MASS" />
    <uses-permission android:name="android.permission.health.WRITE_BASAL_METABOLIC_RATE" />

    <uses-permission android:name="com.health.openscale.READ_WRITE_DATA" />
    <uses-permission android:name="com.health.openscale.oss.READ_WRITE_DATA" />
    <uses-permission android:name="com.health.openscale.beta.READ_WRITE_DATA" />
    <uses-permission android:name="com.health.openscale.debug.READ_WRITE_DATA" />

    <queries>
        <package android:name="com.health.openscale" />
        <package android:name="com.health.openscale.oss" />
        <package android:name="com.health.openscale.beta" />
        <package android:name="com.health.openscale.debug" />
        <provider android:authorities="com.health.openscale.provider" />
        <provider android:authorities="com.health.openscale.oss.provider" />
        <provider android:authorities="com.health.openscale.beta.provider" />
        <provider android:authorities="com.health.openscale.debug.provider" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="ScaleLauncher"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <activity android:name=".DeviceScanActivity" android:exported="false" />

        <activity
            android:name=".HealthPermissionsRationaleActivity"
            android:exported="false" />

        <!-- Android 14+: privacy/rationale link in the Health Connect permission UI. -->
        <activity-alias
            android:name=".ViewHealthPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".HealthPermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ScaleScanService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <receiver android:name=".BootReceiver" android:enabled="true" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
